-- =============================================================================
-- Migration: 给 highlights 表的 id 字段补注释
-- 日期: 2026-05-14
-- 背景:
--   highlights 表原始 SQL 只定义了 11 个字段，但生产 DB 实际多了一个
--   `id INTEGER`（nullable，无约束），由后期手动 SQL 添加。
--   主键仍为 code（语义主键），id 列实际冗余。
--   本 migration 只补 id 字段注释（其余 11 个字段已在原 SQL 中带 COMMENT）。
-- =============================================================================

COMMENT ON COLUMN highlights.id IS '【冗余列】后期手动添加的自增 ID，可空。主键实际为 code（语义主键），此列不参与业务逻辑，仅作管理后台展示兼容';
